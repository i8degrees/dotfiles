# 2011-07/19:jeff
#
#	/home/jeff/.synergy.conf @ libra.syn.localnet
#
# http://synergy2.sourceforge.net/configuration.html
#
section: screens
	virgo:
	libra:
end

section: aliases
	virgo:
		virgo.local		
		#virgo.syn.localnet
	libra:
		libra.local
		#libra.syn.localnet
end

section: links
	virgo:
		left = libra
		right = virgo
	libra:
		left = libra
		right = virgo
end

section: options
	heartbeat = 5000 # milliseconds
	screenSaverSync = false
	switchDelay = 0 # milliseconds
	keystroke(super+tilde) = switchInDirection(right)
	keystroke(control+tilde) = lockCursorToScreen(toggle)
	#keystroke(super+tilde) = switchToScreen(lappy)
	#keystroke(alt+tilde) = switchToScreen(libra)
end

