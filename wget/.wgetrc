# SOURCE: https://github.com/mathiasbynens/dotfiles/blob/master/.wgetrc

# Use the server-provided last modification date, if available
timestamping = on

# Do not go up in the directory structure when downloading recursively
no_parent = on

timeout = 30

# Retry a few times when a download fails; the default is twenty (20)
tries = 3

# Retry even when the connection was refused
retry_connrefused = on

# Use the last component of a redirection URL for the local file name
trust_server_names = on

# Follow FTP links from HTML documents by default
follow_ftp = on

# Add a `.html` extension to `text/html` or `application/xhtml+xml` files that lack one, or a `.css` extension to `text/css` files that lack one
adjust_extension = on

# Use UTF-8 as the default system encoding
# Disabled as it makes `wget` builds that don’t support this feature unusable.
# Does anyone know how to conditionally configure a wget setting?
# http://unix.stackexchange.com/q/34730/6040
# local_encoding = UTF-8

# Ignore `robots.txt` and `<meta name=robots content=nofollow>`
robots = off

# Print the HTTP and FTP server responses
server_response = on

# user_agent = Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)

# Homebrew; MacOS && Linux
#ca-directory = /usr/local/etc/ssl/certs

# standardized search paths; these paths do not
# exist on Android OS :-/
#ca-directory = /etc/ssl/certs

# Google Chrome v134.0.6998.166
#
# 1. https://www.useragentstring.com/Chrome134.0.6998.166_id_20018.php
user-agent = "Mozilla/5.0 (Windows NT 11.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/134.0.6998.166 Safari/537.36"

# Arch / Manjaro Linux
#--ca-directory = /etc/ca-certificates/trust-source/anchors
#--ca-directory = /usr/share/ca-certificates/trust-source/anchors

# Debian Linux
#--ca-directory = /usr/local/share/ca-certificates

# Homebrew; MacOS && Linux
#--ca-directory = /usr/local/etc/ssl/certs

# standardized search paths
#--ca-directory = /etc/ssl/certs
