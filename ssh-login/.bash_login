#!/bin/bash
#
# 2011-09/09:jeff
#
#		~/.bash_login
#

case "$(uname -s)" in
  Darwin)
  ;;
  Linux)
    if [ -r "$HOME/.bash/ssh_login" ]; then
      "$HOME/.bash/ssh_login"
    fi
  ;;
  *)
    # catch all
    true
  ;;
esac

