#!/usr/bin/env bash
#
# 2011-09/09:jeff
#
#		~/.bash_login
#

case "$(uname -s)" in
  Darwin)
  ;;
  Linux)
    if [ -r "$HOME/.bash/ssh_login.sh" ]; then
      "$HOME/.bash/ssh_login.sh"
    fi
  ;;
  *)
    # catch all
    true
  ;;
esac

